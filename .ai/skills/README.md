# Skills

No repo-specific Claude Code skills yet — this repo is small enough that the agents in
`.ai/agents/` plus `.ai/SPEC.md`/`.ai/workflows/` cover current needs. Symlinked as
`.claude/skills` by `.ai/scripts/setup-ai-symlinks.sh` so it's ready the moment a skill is
added here (e.g. a `deploy-github-pages` or `swap-photo` skill), same pattern as
`.ai/agents/`.
